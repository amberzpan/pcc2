<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pcc2.social.mapper.PostMapper">
    <select id="findAll" resultType="com.pcc2.social.entity.Post">
        SELECT p.*, u.username, u.nickname, u.avatar
        FROM post p
        LEFT JOIN user u ON p.user_id = u.id
        ORDER BY p.created_at DESC
        LIMIT #{offset}, #{limit}
    </select>
    
    <select id="findByUserId" resultType="com.pcc2.social.entity.Post">
        SELECT p.*, u.username, u.nickname, u.avatar
        FROM post p
        LEFT JOIN user u ON p.user_id = u.id
        WHERE p.user_id = #{userId}
        ORDER BY p.created_at DESC
        LIMIT #{offset}, #{limit}
    </select>
    
    <select id="findById" resultType="com.pcc2.social.entity.Post">
        SELECT p.*, u.username, u.nickname, u.avatar
        FROM post p
        LEFT JOIN user u ON p.user_id = u.id
        WHERE p.id = #{id}
    </select>
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO post (user_id, content, media_url, media_type, repost_id, original_content, like_count, comment_count, repost_count)
        VALUES (#{userId}, #{content}, #{mediaUrl}, #{mediaType}, #{repostId}, #{originalContent}, 0, 0, 0)
    </insert>
    
    <delete id="delete">
        DELETE FROM post WHERE id = #{id}
    </delete>
    
    <update id="updateLikeCount">
        UPDATE post SET like_count = #{count} WHERE id = #{id}
    </update>
    
    <update id="updateCommentCount">
        UPDATE post SET comment_count = #{count} WHERE id = #{id}
    </update>
    
    <update id="updateRepostCount">
        UPDATE post SET repost_count = #{count} WHERE id = #{id}
    </update>
</mapper>
