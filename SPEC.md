# PCC2 社交平台需求与重构规范（V2）

> 本文档是当前项目唯一需求基线。目标是：在保留原有功能的前提下，修复已知漏洞、补齐测试体系，并新增搜索与个人资料编辑等能力。

## 1. 项目目标

### 1.1 产品目标
- 提供一个前后端分离的轻量社交平台，支持发布、互动、关系链与收藏。
- 在当前代码基础上进行分阶段重构，优先修复认证与接口契约问题。
- 建立可执行测试基线，确保每阶段改动可验证、可回归。

### 1.2 技术栈
- 前端：Vue 3 + Vite + Vue Router + Axios
- 后端：Spring Boot 3.2 + MyBatis + Spring Security
- 数据库：MySQL 8
- 认证：JWT Bearer Token

### 1.3 运行端口
- 后端：`http://localhost:9090`
- 前端：`http://localhost:5173`

## 2. 功能范围

### 2.1 保留并修复的既有功能
- 用户注册、登录、获取当前用户信息
- 帖子发布（文本、图片、视频）、列表、详情、删除、转发
- 评论发布、删除、按时间/热度排序
- 点赞/取消点赞、关注/取消关注、收藏/取消收藏
- 个人主页帖子列表、媒体资源访问

### 2.2 新增功能（本轮必须交付）
- 帖子搜索（按内容与作者信息）
- 个人资料编辑（昵称、简介、头像）
- 收藏页路由与页面（修复现有菜单断链）
- 关注动态流（关注用户帖子聚合）

## 3. 非功能要求

### 3.1 安全要求
- 所有写操作接口（发布、删除、评论、点赞、关注、收藏、资料修改）必须强制鉴权。
- JWT 校验失败统一返回 `401`，无权限操作返回 `403`。
- 用户输入需做最小必要校验（用户名、密码、帖子/评论内容、分页参数）。
- 上传文件限制类型与大小，并避免依赖单一 `Content-Type` 判断。

### 3.2 可维护性要求
- 前后端字段命名统一：帖子媒体字段使用 `mediaUrl` / `mediaType`。
- 响应结构统一使用 `Result<T>`，错误码语义明确。
- 关键业务链路提供自动化测试覆盖。

### 3.3 性能与体验要求
- 列表接口必须分页。
- 前端关键页面首屏可用，错误状态需有可见提示。
- 所有新增页面应兼容移动端基础布局。

## 4. 数据模型与约束

### 4.1 数据库
- 库名：`social_platform`
- 初始化脚本：`database.sql`

### 4.2 核心约束
- `user.username` 全局唯一。
- `like_record(post_id, user_id)` 唯一。
- `follow(follower_id, following_id)` 唯一，且不允许自己关注自己。
- `favorite(user_id, post_id)` 唯一。

## 5. API 契约要求

### 5.1 统一响应
成功：

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

失败：

```json
{
  "code": 400,
  "message": "错误信息",
  "data": null
}
```

### 5.2 鉴权策略
- 公开接口：注册、登录、公开帖子列表、帖子详情、评论列表、公开用户信息。
- 受保护接口：所有写操作及个人相关读取（收藏列表、关注流、个人资料修改）。

### 5.3 本轮新增接口
- `GET /api/post/search?keyword=&page=&size=`：帖子搜索
- `PUT /api/user/info`：更新当前用户资料
- `GET /api/post/following?page=&size=`：关注动态流

## 6. 前端页面与路由要求

### 6.1 必备路由
- `/`：首页信息流
- `/login`：登录
- `/register`：注册
- `/publish`：发布（鉴权）
- `/profile`：我的主页（鉴权）
- `/favorites`：收藏页（鉴权）
- `/search`：搜索结果页

### 6.2 交互要求
- 登录态失效时自动清理本地 token 并跳转登录页。
- 帖子卡片操作（点赞/收藏/关注）状态与计数需一致。
- 评论区排序切换时必须按选项刷新数据。

## 7. 分阶段实施计划

### Phase 0：文档与测试基线
- 重写 `SPEC.md` 为真实需求文档（本文件）。
- 增加后端自动化测试骨架，先覆盖已知漏洞点（认证、参数校验、媒体字段契约）。

### Phase 1：安全与逻辑漏洞修复
- 修复登录 DTO 使用错误。
- 修复 JWT 拦截逻辑（受保护接口无 token 不得放行）。
- 修复/统一输入校验与错误码语义。
- 修复帖子发布 `imageUrl`/`mediaUrl` 兼容问题。

### Phase 2：后端能力增强
- 新增搜索接口、关注流接口、资料更新接口。
- 保持旧功能兼容，并补齐对应测试。

### Phase 3：前端重构与体验优化
- 新增并接入 `FavoritesView`、`SearchView`、资料编辑能力。
- 统一 API 字段消费与错误处理。
- 在保持当前视觉风格基础上进行结构化升级。

### Phase 4：回归与收尾
- 全量执行后端测试与前端构建/测试。
- 修正文档与代码偏差，输出最终验收清单。

## 8. 测试策略（必须执行）

### 8.1 后端自动化测试
- 类型：JUnit 5 + Mockito + Spring MockMvc（按需）
- 覆盖重点：
  - JWT 鉴权拦截
  - 用户注册/登录参数校验
  - 帖子发布媒体字段契约
  - 搜索与关注流返回逻辑

### 8.2 前端验证
- 至少保证 `npm run build` 持续通过。
- 关键流程脚本/手动回归：登录、发布、评论、点赞、收藏、搜索、资料修改。

### 8.3 阶段门禁
- 每个 Phase 完成后必须执行并通过对应验证，再进入下一阶段。

## 9. 验收标准

### 9.1 功能验收
- 既有功能全部可用且无退化。
- 新增功能（搜索、资料编辑、收藏页、关注流）全部可用。

### 9.2 质量验收
- 后端测试通过，前端构建通过。
- 关键漏洞（认证放行、字段错配、路由断链）全部修复。

### 9.3 文档验收
- `SPEC.md` 与实际实现一致，不包含过期状态描述。

## 10. V3 增补章程（2026-04-20）

> 本章程覆盖当前迭代的新增目标，优先级高于旧章节中的冲突项。

### 10.1 本轮范围与非目标
- 本轮必须完成：信息流模式收敛、自动加载无卡顿、转发跳转、设置中心、个人资料增强、头像/背景修改、喜欢列表与收藏列表、前端视觉重构。
- 本轮非目标（延期到 VNext）：私信/好友聊天能力。
- 开发顺序必须遵循：更新规范 -> 补测试 -> 改代码 -> 全量回归。

### 10.2 信息流模式与游客限制
- 信息流模式仅保留：`all`（首页）、`hot`（热门）、`following`（关注）。
- 移除 `discover`（发现动态）相关前后端接口、路由、按钮与文案。
- 游客可浏览首页 `all`，不可切换到 `hot/following`。
- 后端权限约束：`/api/post/hot` 与 `/api/post/following` 未登录访问返回 `401`。

### 10.3 首页排序算法
- 首页 `all` 采用「互动热度 + 时间衰减」排序，默认公式：
  - `all_score = (like_count*1.0 + comment_count*2.0 + repost_count*2.8) / POW(age_hours + 2, 1.25)`
- 热门 `hot` 采用互动强权重：
  - `hot_score = like_count*1.2 + comment_count*2.4 + repost_count*3.0`
- 两个模式都需要在同分情况下按 `created_at DESC` 打破平局。

### 10.4 自动加载与性能体验
- 首页、个人页、收藏页、关系列表改为自动加载（IntersectionObserver），移除“加载更多”点击式交互。
- 页面切换与快速模式切换必须避免串响应（请求取消或结果过期保护）。
- 列表加载需提供骨架态或清晰加载提示，空态文案明确。

### 10.5 转发跳转与帖子详情
- 转发块必须可点击并跳转到原帖详情页。
- 新增帖子详情路由（`/post/:id`），支持查看完整帖子及评论。
- 当原帖不存在时，展示明确错误提示而不是静默失败。

### 10.6 设置中心
- 新增 `/settings` 页面，包含：
  - 主题模式（浅色/深色）
  - 帮助文档入口
  - 修改密码
  - 修改密码校验要求：旧密码必填、新密码长度 8-24、需包含字母与数字、确认密码一致。

### 10.7 个人资料增强
- 资料修改规则：昵称必填，简介选填。
- 个人页需展示注册日期（`createdAt`）。
- 用户资料模型新增：`coverUrl`（背景图）。

### 10.8 头像与背景图修改
- 个人页头像与背景图支持点击上传修改。
- 上传成功后立即回显，保存资料后持久化。

### 10.9 喜欢列表与收藏列表
- 新增“喜欢列表”能力（按点赞时间倒序展示帖子）。
- 喜欢列表与收藏列表展示在个人页标签区，支持分页自动加载。

### 10.10 前端设计与交互规范（impeccable）
- 必须使用图标替代 emoji，不允许再使用表情字符作为导航/功能按钮。
- 视觉目标：sleek、premium、minimalist（Swiss spa 风格），克制配色，统一间距节奏。
- 统一术语与按钮文案，移除冗余表达。
- 桌面与移动端均需优雅适配，不得通过隐藏核心功能完成“适配”。

### 10.11 测试门禁
- 后端新增测试覆盖：
  - 信息流模式权限（游客限制）
  - 首页/热门排序逻辑
  - 修改密码校验链路
  - 资料更新必填项（昵称）
  - 喜欢列表接口
- 前端新增测试覆盖：
  - 首页模式访问限制（游客不可切换关注/热门）
  - 自动加载触发逻辑
  - 转发跳转行为
  - 设置主题持久化与改密表单校验
  - 个人页标签（动态/喜欢/收藏）
- 阶段完成前必须通过：后端测试、前端单测、`npm run build`。
