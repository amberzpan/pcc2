# PCC2 SimpleSocial 当前规格

本文档记录当前实现应满足的产品、接口和体验约定。历史阶段计划、已完成修复说明和过时限制不再作为需求依据。

## 1. 项目目标

- 提供一个前后端分离的轻量社交平台，支持公开浏览、登录互动、发布内容、关系链、收藏和个人主页管理。
- 默认体验参考 X Web 与微博的信息流：中间帖子流滚动，左右栏固定，信息层级清楚，交互反馈克制。
- 游客可以浏览公开内容；登录后可以发布、评论、点赞、收藏、转发、关注和管理个人资料。

## 2. 技术栈与运行

- 前端：Vue 3 + Vite + Vue Router + Axios
- 后端：Spring Boot 3.2 + MyBatis + Spring Security
- 数据库：MySQL `social_platform`
- 鉴权：JWT Bearer Token
- 后端地址：`http://localhost:9090`
- 前端地址：`http://localhost:5173`
- 本地测试账号：`aaa` / `aaa12345`

## 3. 核心功能

- 用户：注册、登录、忘记密码重置、获取当前用户、修改密码。
- 帖子：发布文字、图片、视频、常用表情；浏览列表和详情；删除自己的帖子；转发并跳转原帖。
- 媒体：帖子内图片和视频按比例展示，点击媒体本身即可预览。
- 评论：发布、删除、按时间或热度排序、评论点赞。
- 互动：点赞、收藏、关注、取消对应操作，状态和计数需要及时反馈。
- 信息流：首页、热度、关注三种模式；游客只显示首页模式，登录后可切换热度和关注。
- 个人主页：展示昵称、用户名、注册时间、简介、关注和粉丝数据；支持编辑昵称、简介、头像和背景。
- 列表页：收藏页、搜索页、帖子详情页、设置页、帮助页。
- 右侧栏：首页展示今日热搜和推荐关注；热搜点击直接进入对应帖子详情；推荐关注不包含已关注用户。

## 4. 页面与布局

- `/`：首页信息流。游客仅显示首页内容，不显示模式切换按钮。
- `/login`：登录页，包含忘记密码入口。
- `/register`：注册页。
- `/publish`：发布页，需要登录。
- `/profile`：我的主页，需要登录。
- `/profile/:id`：公开用户主页。
- `/favorites`：收藏页，需要登录。
- `/search`：搜索结果页。
- `/settings`：设置页，需要登录。
- `/post/:id`：帖子详情页。
- `/help`：帮助页。

布局要求：

- 页面不允许横向拖动或横向滚动。
- 信息流滚动时，左侧导航栏和右侧栏保持固定。
- 页面滚动到顶部或底部时不应出现整页被继续拖动的感觉。
- 桌面优先，同时保留移动端基础可用布局。
- 深色模式下文字不能使用刺眼纯白大面积铺开；浅色模式是游客默认主题。

## 5. API 约定

统一成功响应：

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

统一失败响应：

```json
{
  "code": 400,
  "message": "错误信息",
  "data": null
}
```

公开接口：

- `POST /api/user/register`
- `POST /api/user/login`
- `POST /api/user/forgot-password`
- `GET /api/post/list`
- `GET /api/post/hot`
- `GET /api/post/search`
- `GET /api/post/{id}`
- `GET /api/comment/post/{postId}`
- `GET /api/user/{userId}/profile`
- `GET /api/user/search`

受保护接口：

- 发布、删除、上传、点赞、评论写入、收藏、关注、修改资料、修改密码。
- `GET /api/post/following`
- 当前用户信息、收藏列表、喜欢列表等个人相关数据。

鉴权规则：

- 写操作必须登录。
- 受保护接口缺少或携带无效 token 时返回 `401`。
- 公开 GET 接口携带失效 token 时应降级为游客浏览，不应把公开页面强制跳转到登录页。

## 6. 排序与推荐

首页排序：

- 首页 `all` 使用互动热度和时间衰减综合排序。
- 默认公式：`all_score = (like_count*1.0 + comment_count*2.0 + repost_count*2.8) / POW(age_hours + 2, 1.25)`。
- 同分时按 `created_at DESC` 排序。

热度排序：

- 热度 `hot` 使用互动强权重排序。
- 默认公式：`hot_score = like_count*1.2 + comment_count*2.4 + repost_count*3.0`。
- 今日热搜基于热门帖子生成，每条热搜必须保留具体 `postId`，点击进入 `/post/:id`。

推荐关注：

- 基于近期公开帖子和热门帖子中的作者生成候选。
- 排除当前用户和已经关注的用户。
- 按作者相关帖子热度累计分排序。

## 7. 安全与输入

- 用户名、密码、帖子、评论、分页参数都要做必要校验。
- 密码长度为 8-24 位，必须同时包含字母和数字。
- 文件上传需限制类型和大小：
  - 图片最大 5MB。
  - 视频最大 50MB。
- 媒体字段统一使用 `mediaUrl` / `mediaType`，兼容旧 `imageUrl` 消费。

## 8. 前端体验要求

- 导航、发布、收藏、点赞等功能按钮使用图标，不使用 emoji 作为功能图标。
- 收藏按钮在帖子操作区最右侧，仅保留图标。
- 点赞、转发、收藏等按钮点击后需要有状态颜色变化。
- 发布页的表情使用常规微博/微信式常用表情，表情面板不应过大，点击表情后不自动收起。
- 页面切换尽量避免不必要的“加载中”闪烁，已有内容可继续保留直到新数据返回。
- 右侧热搜和推荐关注不可内部滚动；跟随页面保持固定布局。
- 注册、登录、个人主页、设置页也属于需要打磨的核心界面。

## 9. 验证要求

每次功能修改后至少执行：

- 前端构建：`npm run build`
- 前端测试：`node --test src/views/__tests__/*.spec.js`
- 后端编译：`mvn -q -DskipTests compile`

关键手动回归：

- 游客打开首页。
- 登录、注册、忘记密码、退出登录。
- 发布文字、图片、视频和表情。
- 点赞、评论、收藏、转发、关注。
- 首页、热度、关注、搜索、收藏、个人主页和帖子详情。
